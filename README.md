# 🎵 Playlist Generator — JavaScript Capstone Lab

## Setup

### 1. Clone this repository

```bash
git clone https://github.com/YOUR-INSTRUCTOR-USERNAME/playlist-generator.git
cd playlist-generator
```

### 2. Open in VS Code

```bash
code .
```

### 3. Run with Live Server

- Install the **Live Server** extension in VS Code if you haven't already
- Right-click `index.html` → **Open with Live Server**
- The app opens at `http://127.0.0.1:5500`

> ⚠️ You **must** use Live Server — not just open the file directly. The app uses JavaScript modules which require a local server to work.

## File Structure

| File | Your Role |
|---|---|
| `index.html` | ✅ Complete — do not modify |
| `styles.css` | ✅ Complete — do not modify |
| `sidebar.js` | ✅ Complete — do not modify |
| `track.js` | 📝 Build 1 and Build 5 live here |
| `app.js` | 🐛 15 bugs + Build 2–6 stubs |

## Assignment

See the full assignment on Notion — your instructor will provide the link.

## Tips

- Open the browser **Developer Tools → Console** (right-click → Inspect → Console tab) to see error messages as you work
- Use **Ctrl+S** or **Cmd+S** to save your playlist to the sidebar
- Use the **Export** button to download your playlists as a backup `.json` file between class periods
